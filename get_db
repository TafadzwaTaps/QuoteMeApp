from fastapi import Depends
from sqlalchemy.orm import Session
from database import SessionLocal  # or wherever your SessionLocal is defined

def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
